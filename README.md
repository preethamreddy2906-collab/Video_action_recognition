# Video_action_recognition
Video Action Recognition using CNN + LSTM and Self-Attention  This notebook implements and compares two temporal modeling approaches for video action recognition:  • CNN + LSTM   • CNN + Self-Attention    The models are trained on 10 action classes and compared using validation accuracy and training time.
